#  Лабораторная работа №2. Работа с базой данных в Node.js (Express + PostgreSQL)

##  Цель работы
- Освоить создание REST API с использованием **Node.js**, **Express** и **PostgreSQL**.  
- Научиться работать с ORM **Sequelize** для описания моделей и миграций.  
- Реализовать операции **CRUD** (создание, чтение, обновление, удаление).  
- Добавить фильтрацию, поиск, пагинацию и документацию API с помощью **Swagger**.  

## Стек технологий
- **Node.js** v22  
- **Express.js**  
- **PostgreSQL 17**  
- **Sequelize ORM**  
- **Swagger UI Express**  
- **dotenv**

##  Структура проекта

```

todo-app/
│
├── app.js                      # Точка входа
├── .env                        # Переменные окружения
├── config/
│   └── config.json             # Настройки Sequelize
│
├── models/
│   ├── category.js             # Модель категорий
│   └── todo.js                 # Модель задач
│
├── migrations/
│   ├── 0251208092253-create-categories.js
│   └── 20251208101905-create-todos.js
|   └── 20251208103627-create-category.js
|   └── 20251208103656-create-todo.js
│
├── controllers/
│   ├── categoryController.js   # Логика категорий
│   └── todoController.js       # Логика задач
│
├── routes/
│   ├── categoryRoutes.js       # Роуты категорий
│   └── todoRoutes.js           # Роуты задач
│
└── swagger/
└── swagger.js              # Конфигурация Swagger

````
<img width="654" height="524" alt="image" src="https://github.com/user-attachments/assets/5afb3eda-f576-4411-a70c-74aa556c9725" />

##  Установка и запуск

1. Установиkf зависимости:

   ```bash
   npm install
   ```

3. Настроить базу данных PostgreSQL:

   * Создала базу `todo_db` через PgAdmin.
   * В `.env` :

     ```
     DB_USER=postgres
     DB_PASSWORD=your_password
     DB_NAME=todo_db
     DB_HOST=127.0.0.1
     DB_PORT=5433
     ```
<img width="256" height="896" alt="image" src="https://github.com/user-attachments/assets/e14208e8-a209-4f3a-9503-1867c001cf84" />

4. Применила миграции:

   ```bash
   npx sequelize-cli db:migrate
   ```
<img width="926" height="262" alt="image" src="https://github.com/user-attachments/assets/3f5149b7-42a1-4938-98bd-6f918abcac03" />

5. Запустила сервер:

   ```bash
   node app.js
   ```

6. Проверила:

   * Swagger: 👉 [http://localhost:3000/api-docs](http://localhost:3000/api-docs)
     <img width="936" height="703" alt="image" src="https://github.com/user-attachments/assets/c9ffa3e7-af84-4c91-b5f8-1d794dcfd88a" />

   * Категории: 👉 [http://localhost:3000/api/categories](http://localhost:3000/api/categories)
     <img width="832" height="685" alt="image" src="https://github.com/user-attachments/assets/103f9884-c09b-4fb8-b2fc-80cdaab206ce" />
<img width="820" height="674" alt="image" src="https://github.com/user-attachments/assets/e369432f-3055-4dda-86bc-845006c51eea" />

   * Задачи: 👉 [http://localhost:3000/api/todos](http://localhost:3000/api/todos)
     


##  Примеры запросов

### Категории

| Метод    | URL                   | Описание                  |
| -------- | --------------------- | ------------------------- |
| `GET`    | `/api/categories`     | Получить список категорий |
| `GET`    | `/api/categories/:id` | Получить категорию по ID  |
| `POST`   | `/api/categories`     | Создать категорию         |
| `PUT`    | `/api/categories/:id` | Обновить категорию        |
| `DELETE` | `/api/categories/:id` | Удалить категорию         |

Пример POST-запроса:

```json
{
  "name": "Учёба"
}
```


### Задачи (todos)

| Метод    | URL                     | Описание                       |
| -------- | ----------------------- | ------------------------------ |
| `GET`    | `/api/todos`            | Получить список задач          |
| `GET`    | `/api/todos/:id`        | Получить задачу по ID          |
| `POST`   | `/api/todos`            | Создать новую задачу           |
| `PUT`    | `/api/todos/:id`        | Изменить задачу                |
| `PATCH`  | `/api/todos/:id/toggle` | Переключить статус `completed` |
| `DELETE` | `/api/todos/:id`        | Удалить задачу                 |

Пример POST-запроса:

```json
{
  "title": "Подготовить отчёт по проекту",
  "category_id": 1
}
```

---

##  Фильтрация, поиск и пагинация

| Пример запроса                                                | Описание                |
| ------------------------------------------------------------- | ----------------------- |
| `/api/todos?category=1`                                       | Фильтрация по категории |
| `/api/todos?search=отчёт`                                     | Поиск по названию       |
| `/api/todos?sort=title:asc`                                   | Сортировка по названию  |
| `/api/todos?page=2&limit=1`                                   | Пагинация               |
| `/api/todos?search=проект&sort=createdAt:desc&page=1&limit=5` | Комбинация параметров   |

Ответ API:

```json
{
  "data": [ /* массив задач */ ],
  "meta": {
    "total": 10,
    "count": 5,
    "limit": 5,
    "pages": 2,
    "currentPage": 1
  }
}
```

##  Swagger-документация

Swagger доступен по адресу:
 [http://localhost:3000/api-docs](http://localhost:3000/api-docs)

В нём представлены все эндпоинты с возможностью тестирования прямо из браузера:

* Категории (GET, POST, PUT, DELETE)
* Задачи (GET, POST, PATCH, DELETE)
* Примеры JSON-запросов и ответов.


##  Контрольные вопросы

1. Что такое реляционная база данных и её преимущества?
   → Это база данных, использующая таблицы и связи между ними, что обеспечивает целостность данных и удобные SQL-запросы.

2. Какие типы связей существуют в реляционных БД?
   → Один-к-одному (1:1), один-ко-многим (1:N), многие-ко-многим (M:N).

3. Что такое RESTful API и зачем он нужен?
   → Это архитектурный стиль для построения веб-сервисов, позволяющий клиенту и серверу взаимодействовать через стандартные HTTP-запросы.

4. Что такое SQL-инъекция и как защититься?
   → Это попытка внедрить вредоносный SQL-код в запрос. Защита — использовать параметризованные запросы и ORM.

5. Чем ORM отличается от сырых SQL-запросов?
   → ORM (например, Sequelize) абстрагирует SQL и упрощает работу с БД, обеспечивая безопасность и кросс-платформенность.


##  Вывод

В ходе лабораторной работы реализовано полноценное REST API-приложение для управления задачами с поддержкой:

* PostgreSQL и ORM Sequelize,
* CRUD-операций,
* фильтрации, поиска и пагинации,
* автоматической Swagger-документации.

Приложение успешно протестировано через браузер, Postman и Swagger UI.

